// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: queries.sql

package repository

import (
	"context"
	"database/sql"
)

const countUserByEmail = `-- name: CountUserByEmail :one
SELECT
    count(*)
FROM
    ` + "`" + `users` + "`" + `
WHERE
    ` + "`" + `email` + "`" + ` = ?
`

func (q *Queries) CountUserByEmail(ctx context.Context, email string) (int64, error) {
	row := q.db.QueryRowContext(ctx, countUserByEmail, email)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createUser = `-- name: CreateUser :execresult
INSERT INTO
    ` + "`" + `users` + "`" + ` (` + "`" + `email` + "`" + `, ` + "`" + `name` + "`" + `, ` + "`" + `password` + "`" + `, ` + "`" + `refresh_token` + "`" + `)
VALUES
    (?, ?, ?, ?)
`

type CreateUserParams struct {
	Email        string
	Name         string
	Password     string
	RefreshToken sql.NullString
}

func (q *Queries) CreateUser(ctx context.Context, arg CreateUserParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, createUser,
		arg.Email,
		arg.Name,
		arg.Password,
		arg.RefreshToken,
	)
}

const findAllUsers = `-- name: FindAllUsers :many
SELECT
    id, email, name, password, refresh_token
FROM
    ` + "`" + `users` + "`" + `
`

func (q *Queries) FindAllUsers(ctx context.Context) ([]User, error) {
	rows, err := q.db.QueryContext(ctx, findAllUsers)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Email,
			&i.Name,
			&i.Password,
			&i.RefreshToken,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const findUserByEmail = `-- name: FindUserByEmail :one
SELECT
    id, email, name, password, refresh_token
FROM
    ` + "`" + `users` + "`" + `
WHERE
    ` + "`" + `email` + "`" + ` = ?
`

func (q *Queries) FindUserByEmail(ctx context.Context, email string) (User, error) {
	row := q.db.QueryRowContext(ctx, findUserByEmail, email)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.Name,
		&i.Password,
		&i.RefreshToken,
	)
	return i, err
}

const findUserById = `-- name: FindUserById :one
SELECT
    id, email, name, password, refresh_token
FROM
    ` + "`" + `users` + "`" + `
WHERE
    ` + "`" + `id` + "`" + ` = ?
`

func (q *Queries) FindUserById(ctx context.Context, id uint8) (User, error) {
	row := q.db.QueryRowContext(ctx, findUserById, id)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.Name,
		&i.Password,
		&i.RefreshToken,
	)
	return i, err
}

const getLastInsertedId = `-- name: GetLastInsertedId :one
SELECT
    LAST_INSERTED_ID ()
`

func (q *Queries) GetLastInsertedId(ctx context.Context) (interface{}, error) {
	row := q.db.QueryRowContext(ctx, getLastInsertedId)
	var last_inserted_id interface{}
	err := row.Scan(&last_inserted_id)
	return last_inserted_id, err
}
