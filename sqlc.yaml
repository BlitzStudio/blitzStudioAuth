version: "2"
sql:
  - engine: "mysql"
    queries: "./db/queries/queries.sql"
    schema: "./db/migrations/000001_create_users_table.up.sql"
    gen:
      go:
        package: "repository"
        out: "out/repository"
